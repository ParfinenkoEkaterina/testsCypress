{
  "name": "testscypress",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "cypress open",
    "testHeadlessChromeMobile": "cypress run --config-file cypress.mobile.js --headless --browser chrome",
    "testHeadedChromeMobile": "cypress run --config-file cypress.mobile.js --headed --browser chrome",
    "testHeadlessElectronMobile": "cypress run --config-file cypress.mobile.js --headless --browser electron",
    "testHeadedElectronMobile": "cypress run --config-file cypress.mobile.js --headed --browser electron",
    "testHeadlessChromeLaptop": "cypress run --config-file cypress.laptop.js --headed --browser chrome",
    "testHeadedChromeLaptop": "cypress run --config-file cypress.laptop.js --headed --browser chrome",
    "testHeadlessElectronLaptop": "cypress run --config-file cypress.laptop.js --headed --browser electron",
    "testHeadedElectronLaptop": "cypress run --config-file cypress.laptop.js --headed --browser electron"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.6.1"
  },
  "description": ""
}
